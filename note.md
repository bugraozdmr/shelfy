# npm sürümleri eskimişse otomatik güncelleyen araç
```
npx npm-check-updates -u
npm install
```

# Render Deploy
*Settings > Build Command*
`npm install && npx prisma generate`

*Start Command*
`node server.js`


# Verdiği rota
`https://shelfy-znau.onrender.com`